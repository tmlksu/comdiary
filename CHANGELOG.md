# 変更履歴

## 0.3.0 — 2026-08-20

Gemini API (Google AI Studio) を LLM バックエンドとして使えるようにした。

Copilot CLI は定額なので呼び出し回数を気にせず書けたが、従量課金の API に載せると
コストは**送ったトークン数**で決まる。1つの会議は「1回の分割 + セグメント数ぶんの抽出」で
処理され、そのすべてが議事録全文を必要とするため、素朴に移植すると全文が
セグメント数+1 回ぶん課金される。そこを潰した上での対応。

### 追加

- **`backend = "gemini"`** — Google AI Studio の API キー方式。追加の依存は無く、
  REST を `urllib` で叩く。低スペックな常時稼働機で動かすため依存を軽く保つ方針
  (→ pyproject) を崩さないため。Vertex AI のサービスアカウント認証には未対応
- **context caching** — 議事録を1回だけアップロードし、以降の呼び出しはそれを参照する。
  文書を処理し終えた時点でキャッシュを削除する (生きている間ずっと課金されるため)。
  短すぎてキャッシュできない議事録と、作成に失敗した場合は全文送信に落ちる
- **`responseSchema` による制約デコード** — スキーマを API に直接渡すので、数 kB の
  JSON Schema を毎回プロンプトに載せる必要がなく、検証失敗による再試行もほぼ起きない。
  `src/comdiary/llm/schema.py` が pydantic の JSON Schema を Gemini の
  OpenAPI サブセットに変換する ($ref の展開、null 分岐の `nullable` への畳み込み)。
  表現しきれないスキーマは黙って落とさず、従来のプロンプト方式にフォールバックする
- **`[llm.gemini]` 設定テーブル** — API キーは設定ファイルに書かず、
  `api_key_env` で**環境変数名だけ**を指定する。設定ファイルはマシン間でコピーされるため

### 変更

- `comdiary doctor` の LLM 点検をバックエンド自身の責務にした。
  CLI なら PATH 上のバイナリ、API なら環境変数のキーと、見るべきものが違うため。
  `--probe` は従量課金では実際に課金される旨をその場で表示する
- 議事録をプロンプトに埋め込まず、バックエンドに `context` として別に渡すようにした。
  キャッシュできるバックエンドが「1回アップロードして使い回す」を選べるようにするため。
  埋め込む場合の文面は従来と1バイトも変わらない (`llm.backend.compose_prompt`)

### 破壊的変更 (内部 API)

自作バックエンドや `llm/prompts.py` を直接呼んでいる場合のみ影響します。

- `LLMBackend` に `context` 引数と `preflight()` / `probe()` /
  `release_context()` / `close()` が加わった
- `split_prompt()` / `detail_prompt()` から議事録本文の引数が無くなった

### 注意

Gemini は従量課金です。まず `--llm fake` と `--dry-run` で配線を確かめ、
`comdiary doctor` で環境変数を確認してから実キーに切り替えてください。
台帳の過去分はそのまま使えます (`comdiary rerender` は LLM を呼ばず、
各会議の JSON には使ったバックエンドとモデルが記録されています)。

## 0.2.0 — 2026-08-20

案件になる前の論点を扱えるようにした。

会議から新しい案件が生まれる過程が扱えていなかった。`_inbox` は「振り分け忘れ」の
置き場であって「まだ案件になっていない話題」の置き場ではなく、案件は排他的帰属なので
複数案件にまたがる論点も表現できなかった。

### 追加

- **`comdiary topics`** — 論点を会議・案件をまたいで集計する。
  頻度より**またぎの広さ**を優先して並べる。1案件で5回出る話はその案件の通常業務だが、
  2案件で1回ずつ出る話は仕組みの問題の候補のため。案件未割当のセグメントも
  「まだどこにも属していない」ものとしてまたぎに数える
- **`comdiary topics --show <論点>`** — 出現箇所・関係者・根底にある関心事・
  未解決事項・温度感をまとめて出す。提案を組み立てるための材料
- **`Segment.topics`** — 論点をセグメント自体の属性として pass2 で抽出する。
  `Signal.topic` とは別に持つ: signals は「特筆すべき場合のみ」記録するため、
  それを集計キーにすると声の大きい話題しか候補に出ず、静かに繰り返される
  話題ほど埋もれてしまう
- MCP に `topics` / `topic_get` を追加

### 変更

- 既出の論点を抽出プロンプトに戻し、表記を寄せる。同じ内容に別の言い方が当たると
  集計が分断されるため
- 会議正本とセグメントメモに論点を表示する

### 既知の限界

集計は**ラベルの文字列一致**でしか繋ぎません。同じ根っこの問題でも現れ方が違えば
別の論点として並びます(「移行手順書」「デザイン意図」「レビュー体制」が
どれも属人化の話であっても統合しない)。「これらは同じ問題だ」は抽出ではなく解釈で、
機械が勝手に束ねると根拠のない繋がりが台帳に残るため、意図的にそうしています。
`topics` の一覧はその判断をするための材料として使ってください。

### 注意

`topics` は**新しく取り込んだ議事録にのみ効きます**。0.1.x で取り込み済みの
議事録には `topics` フィールドがありません。集計に含めたい場合は取り込み直してください。

## 0.1.1 — 2026-08-19

Windows での初回セットアップが通らない不具合の修正と、日時解決の見直し。

### 修正

- **`comdiary init` の直後に `doctor` が TOML の解析エラーで落ちる (Windows)** —
  台帳パスを TOML の基本文字列(ダブルクォート)に書いていたため、
  `ledger = "C:\Users\..."` の `\U` が Unicode エスケープとして解釈されていた。
  パスはリテラル文字列(シングルクォート)で書き出すようにした。
  設定テンプレートのパスもすべてシングルクォートに変更
- **設定ファイルの解析に失敗すると traceback が出るだけだった** —
  ファイル名・原因・対処を添えたエラーにした

### 変更

- **日付だけのファイル名に対応** — 文字起こしのファイル名が `yyyy-mm-dd` 形式で
  時刻を含まない場合、これまでは同じ日の会議がすべて 00:00 になり、
  時系列の順序が失われていた。日付と時刻を別々の出所から解決し、
  時刻が無ければ**同じ日付の**時刻を本文や LLM から借りるようにした。
  発言ごとのタイムスタンプ (`00:03:12 田中: ...`) を開始時刻と誤認しないよう、
  時刻だけの記載はラベル付き (`日時:` `開始時刻:` 等) の場合のみ拾う
- **会議日時の出所を記録** — `date_source` / `time_source` を `Meeting` に追加。
  ファイルの更新時刻に落ちた場合は警告を出す。文字起こしが会議より後に
  書き出される経路では、更新時刻は会議の時刻ではないため。
  時刻不明の会議は `2026-08-13 (時刻不明)` と描画し、00:00 とは書かない
- **`quiet_seconds` の既定を 900 から 60 に短縮** — 完成した文書を1回で書き出す
  経路なら、同期が届く程度の待ちで足りる。追記型の経路では書き込み間隔より
  長くする必要があることをドキュメントに明記
- パッケージのバージョンを `comdiary.__version__` の一元管理にした

## 0.1.0 — 2026-08-13

最初のリリース。

### できること

- **議事録の取り込み** — フォルダを定期的に見に行き、未処理の文字起こしを最大 N 件処理する
  (`comdiary ingest run`)。原文アーカイブ → 分割 → 案件マッチ → 抽出 → 描画 → 索引 →
  最後に完了フォルダへ移動、の順で、途中で落ちても入力を失わない
- **1つの会議を複数案件に振り分け** — 昼会や定例が複数案件を含むのが常態なので、
  会議は正本として1本保存し、セグメント単位で案件配下に投影する
- **温度感(signals)の記録** — 誰が何にどう反応したかを固定語彙 8 種で記録し、
  人・論点ごとに集計できる (`comdiary concerns`)
- **共通マイクの検知** — 発言比率を機械的に計算し、話者ラベルが信用できない場合は
  論点を残したまま話者帰属だけを落とす
- **チャット / メールの後付け取り込み** — ファイル・フォルダ・標準入力から
  (`comdiary ingest add`)
- **検索と context pack** — SQLite FTS5 による全文検索と、
  資料作成・アジェンダ作成・タスク整理・引き継ぎの目的別コンテキスト生成
- **薄い MCP サーバ** — 読み取り 8 tools + 追記 1 tool。mcp SDK の 1.x / 2.0 両対応

### 設計

`docs/adr/` に判断の記録があります。要点:

- LLM は検証済み JSON だけを返し、Markdown はコードが決定論的に生成する
- 機械が書く領域を HTML コメントで囲み、人が書いた文章を保護する
- 案件を無人で作らない。未割当は `_inbox` に落として人が確認する
- ベクトル検索を使わない (SQLite FTS5 のみ、pure Python 依存のみ)
- 台帳はローカル専用。remote が設定されているとコミットを拒否する

### 動作環境

Python 3.11+ / Windows・Linux・macOS。LLM バックエンドは GitHub Copilot CLI。
