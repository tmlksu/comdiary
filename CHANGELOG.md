# 変更履歴

## 0.1.0 — 2026-08-13

最初のリリース。

### できること

- **議事録の取り込み** — フォルダを定期的に見に行き、未処理の文字起こしを最大 N 件処理する
  (`comdiary ingest run`)。原文アーカイブ → 分割 → 案件マッチ → 抽出 → 描画 → 索引 →
  最後に完了フォルダへ移動、の順で、途中で落ちても入力を失わない
- **1つの会議を複数案件に振り分け** — 昼会や定例が複数案件を含むのが常態なので、
  会議は正本として1本保存し、セグメント単位で案件配下に投影する
- **温度感(signals)の記録** — 誰が何にどう反応したかを固定語彙 8 種で記録し、
  人・論点ごとに集計できる (`comdiary concerns`)
- **共通マイクの検知** — 発言比率を機械的に計算し、話者ラベルが信用できない場合は
  論点を残したまま話者帰属だけを落とす
- **チャット / メールの後付け取り込み** — ファイル・フォルダ・標準入力から
  (`comdiary ingest add`)
- **検索と context pack** — SQLite FTS5 による全文検索と、
  資料作成・アジェンダ作成・タスク整理・引き継ぎの目的別コンテキスト生成
- **薄い MCP サーバ** — 読み取り 8 tools + 追記 1 tool。mcp SDK の 1.x / 2.0 両対応

### 設計

`docs/adr/` に判断の記録があります。要点:

- LLM は検証済み JSON だけを返し、Markdown はコードが決定論的に生成する
- 機械が書く領域を HTML コメントで囲み、人が書いた文章を保護する
- 案件を無人で作らない。未割当は `_inbox` に落として人が確認する
- ベクトル検索を使わない (SQLite FTS5 のみ、pure Python 依存のみ)
- 台帳はローカル専用。remote が設定されているとコミットを拒否する

### 動作環境

Python 3.11+ / Windows・Linux・macOS。LLM バックエンドは GitHub Copilot CLI。
